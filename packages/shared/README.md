# @chronofrost/shared

Small dependency-free package for types/constants that both the browser and server can import later.

Current rule: keep this package pure and lightweight. Do not add Solana, Phaser, Fastify, DB, or Node-only runtime dependencies here.
